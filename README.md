# cinemapedia_app

# Steps for run the app:

1. Copuy the .env.template file and rename it to .env
2. Get an API key from https://www.themoviedb.org/ and paste it in the .env file like this: THE_MOVIEDB_KEY=your_api_key
3. Run the app using `flutter run` command in the terminal
